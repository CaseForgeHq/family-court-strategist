# Files & AI glass and typography — 0.14.23

The scan cards now use the Case desk surface tokens, translucent gradient, border, shadow and backdrop blur in both light and dark scenes. Card headers retain readable contrast without an opaque background. Reference, filename, scan status and file type all use the description type scale (15px, weight 500). The card right edge aligns with the description action; the scroll rail occupies a separate 9px gutter.

Validation: 438 automated tests passed, 4 skipped. Ten native fictional-profile captures passed, including light/dark modes and compact layouts. Computed glass properties match Case desk; font weights/sizes and right-edge alignment are asserted. Native update arrival, keyboard access, download states and three layouts passed. No live AI call was used. Publication and installed-upgrade verification are separate.

Final installer: 196533653 bytes; SHA256 d51c6d677c050a35e8af45b398ba998788c64fc857a9b1d7d15b5ea69c2a88ad. Authenticode: NotSigned. Three isolated installed-copy restart rehearsals reached readiness in 3875, 2481 and 2587 ms; preparation occurred while open. This does not establish an upgrade of the user's running installation.
